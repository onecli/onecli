# Never auto-install peer dependencies: pnpm 9's auto-install materializes
# even OPTIONAL peers of production deps (better-auth alone peers on next,
# react, vue, svelte, vitest, ...), dragging whole frameworks into every
# install — and into the production images' pruned node_modules. Real peer
# needs are declared as real dependencies.
auto-install-peers=false
