CHANGELOG.md

# Generated app-permission catalog the gateway embeds (derived from the TS catalog; drift-checked in packages/api/src/apps/app-permissions/catalog-json.test.ts)
apps/gateway/crates/policy-engine/src/catalog.generated.json

# Nested worktree checkouts (Claude Code sessions) — each is its own repo view;
# the repo-wide glob must never format-check another checkout's files.
.claude/worktrees/

# Conductor workspace scratch space (gitignored agent collaboration files) —
# not repo content; the format gate must not fail on it.
.context/
