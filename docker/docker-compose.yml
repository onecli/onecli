services:
  onecli:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "10254:10254"
      - "10255:10255"
    volumes:
      - onecli-data:/app/data
    env_file: ../.env

volumes:
  onecli-data:
