//! Foundation utilities shared across the gateway workspace: the runtime
//! edition switch, pure helpers, the version string, and the legacy
//! project→workspace wire compatibility.

pub mod compat;
pub mod edition;
pub mod util;
pub mod version;
