{
  "name": "@onecli/gateway",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "dev": "cargo run -- --enable-remote-access --relay-url wss://rat1.lesspassword.dev",
    "check": "cargo clippy -- -D warnings && cargo fmt --check",
    "lint": "cargo fmt --check",
    "format:check": "cargo fmt --check",
    "check-types": "cargo clippy -- -D warnings",
    "test": "cargo test"
  }
}
